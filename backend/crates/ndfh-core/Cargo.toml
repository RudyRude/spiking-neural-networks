[package]
name = "ndfh-core"
version = "0.2.0"
edition = "2021"
description = "NDF-H Core types: membership ledger and minimal hypergraph snapshot types"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
